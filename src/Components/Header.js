import React from 'react';

import '../style.css';

const Header = props => (
    <div className="bg-header">
        <div className="menu-container">
            <h1 className="title">Livraria</h1>
        </div>
    </div>
)

export default Header;